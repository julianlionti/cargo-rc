// Dummy configuration data (e.g., mock environment variables)
export const appConfig = {
  appName: "Cargo RC",
  version: "1.0.0",
  apiUrl: "https://api.example.com",
  debug: true,
};

// Example configuration for a feature toggle
export const featureFlags = {
  newDashboard: true,
  betaFeatures: false,
};
